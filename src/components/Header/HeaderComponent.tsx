const HeaderComponent = () => {
    return (
        <header>
            [toggler]

            [all movies]

            [genres list]

            [search movies]

            [user accaunt info
                * [favourite movies list] (ADDITIONAL)
            ]
        </header>
    );
};

export default HeaderComponent;